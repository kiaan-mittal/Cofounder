Self-hosted variable fonts so `next build` does not fetch from fonts.googleapis.com.

- `Fraunces-Variable.woff2` — latin, full axes (`opsz`, `wght`, `SOFT`, `WONK`)
- `InstrumentSans-Variable.woff2` — latin, `wght`
- `JetBrainsMono-Variable.woff2` — latin, `wght`

Each face is SIL Open Font License 1.1. See the matching `OFL-*.txt` file.
